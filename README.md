# marketplace-mini-site

Deploy repo for the **mini-site** Inkress-embedded app. Generated from `jamlance/bookerva-apps` — edit there, then re-run `scripts/sync-app-repo.mjs mini-site --push`.

Coolify: base_directory `/`, dockerfile `/apps/mini-site/Dockerfile`.
